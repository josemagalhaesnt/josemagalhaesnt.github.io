import styled from 'styled-components';
import { Button } from '..';

export const Container = styled(Button)`
    width: 100%;
    height: 100%;

    &::link {
        text-decoration: none;
    }
`;
